# Travel Agent v7 文档更新说明

> 2026-08-11

本包是对当前 `travel_agent_v7` 文档体系的架构更新覆盖层。

# 本次确定的长期架构

```text
Frontend Agent Runtime
→ @langchain/react

Preferred Chat UX
→ assistant-ui

Preferred adapter
→ @assistant-ui/react-langchain

Product Backend
→ FastAPI

Backend integration
→ FastAPI Agent Streaming Adapter

Agent Runtime
→ Deep Agents / LangGraph

Business State
→ PostgreSQL

Agent Runtime State
→ LangGraph Persistence
```

# 重要边界

assistant-ui 不是硬架构 Owner。

真正的硬边界：

```text
@langchain/react
FastAPI Product Lifecycle
Deep Agents / LangGraph
Business Conversation != Agent Thread
```

assistant-ui 必须经过版本兼容性 Spike；如果失败，只替换 Presentation Layer。

# 为什么不直接 Agent Server-first

Agent Server 是有效替代方案，但当前 v7 已经拥有 Full-Stack FastAPI Product Runtime、Auth/User/DB/Business Modules。

P0 不为了 Streaming 推翻这些资产。

未来若采用 Agent Server-first，必须 ADR。

# 为什么不使用 @ai-sdk/react

当前 Agent Runtime 已是 LangGraph / Deep Agents，`@langchain/react` 能直接消费其 Agent/Thread/Tool/SubAgent 语义。

避免维护第二套 Chat Runtime 和额外协议翻译。

# GitHub 写入状态

当前会话的 GitHub integration 对目标仓库写操作返回 403，因此本次以可覆盖 ZIP 交付。

将 ZIP 解压到仓库根目录，覆盖对应 Markdown 文件即可。
