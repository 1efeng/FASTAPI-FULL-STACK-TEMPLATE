# Travel Agent v8.2 测试策略

> 更新时间：2026-08-13

# 1. Test Layers

```text
Unit
Contract
Integration
E2E
Failure Injection
AI Eval
Load / Capacity
```

# 2. Product Runtime Unit / Integration

必须覆盖：

```text
ownership
idempotency
concurrent duplicate
start TX rollback
server-authoritative history projection
deadline
cancel vs final race
late final after cancel dropped
assistant final commit
Product error mapping
rate/quota/concurrency
RequestRun status API
```

# 3. Agent Contract

```text
PydanticAIExecutor implements AgentExecutor
AgentExecutionRequest mapping
Product history → Pydantic message_history
absolute deadline
CancellationToken
usage extraction
sanitized error mapping
Pydantic types do not leak into Product modules
```

# 4. StreamResumeStore Contract

Memory/test backend 与 Redis implementation 都验证：

```text
single active producer claim
start
resume
start_or_resume
backlog replay
live continuation
cross-context subscriber
skip_characters
FAILED
INTERRUPTED
resume ack timeout
close does not close shared app Redis client
```

# 5. Real Redis Integration

不能只跑 memory/fakeredis。

必须真实 Redis：

```text
SET NX race
Pub/Sub subscription
two contexts / replicas
TTL expiry
subscriber reconnect
transient Redis failure
Pub/Sub listener failure/restart
```

# 6. Streaming Lifecycle Ordering

强制断言：

```text
non-terminal chunk
→ 可早于 Product final COMMIT

success terminal event
→ 绝不能早于 Product final COMMIT
```

并测试：

```text
Product COMMIT success
+
terminal transport emit failure
→ Product remains completed
→ client can reconcile
```

# 7. Disconnect

必须回归：

```text
SSE/browser disconnect
→ subscriber ends
→ Agent execution continues
→ RequestRun remains running
```

这是 M8 对当前 ChatService 的关键行为修正测试。

# 8. Product Stop

```text
POST cancel
→ RequestRun cancelled wins CAS
→ Agent cancellation signal
→ late final dropped
```

前端测试：

```text
Product Stop != useChat.stop()
```

# 9. Full M8 E2E

真实链路：

```text
React/Vite
AI SDK
FastAPI
PydanticAI
Redis
PostgreSQL
Playwright
```

场景：

```text
normal stream
F5 mid-stream
network reconnect
reconnect lands replica B
multiple reconnects
Product Stop
cancel/final race
producer exception
DB commit failure
terminal emit failure
Redis restart/failure
graceful shutdown
producer process hard kill
```

M8 hard kill 允许：

```text
execution lost
```

但不允许：

```text
RequestRun 永远 running
前端永远 spinner
```

# 10. Frontend

```text
ProductChatTransport request mapping
auth/idempotency
active stream rendering
F5 resume
full replay no duplicate assistant
204 reconciliation
Product Stop
durable completed reconciliation
production build
AI Elements Package Gate
```

# 11. Persistence

如果 UI 收到 success terminal：

```text
assistant final must already be durable
RequestRun must already be completed
```

failed/cancelled partial output：

```text
must not appear as durable assistant Message
```

# 12. Load / Capacity

分层：

```text
API/SSE
Redis Pub/Sub
PostgreSQL lifecycle
LiteLLM gateway
mock Agent execution
real model controlled sample
```

关键指标：

```text
peak RPS
active SSE/subscribers
concurrent executions
reconnect rate
Pub/Sub messages
Redis connections
model latency/tokens
DB pool
```

# 13. CI

PR：

```text
unit
contract
selected integration
lint/typecheck
frontend build
```

main/release：

```text
real Redis integration
backend integration
Playwright
migration check
security
Basic Eval
smoke
```

重大 M8/infra upgrade：

```text
failure drill
multi-replica E2E
load test
```
