# Travel Agent v8 施工路线图

> 更新时间：2026-08-13
> v8 clean baseline 已建立。历史 v7 施工记录见 Git 历史，不再维护。

状态图例：

```text
✅ 已完成并验证
🟡 部分完成 / 待下一阶段恢复
SUPERSEDED 正式废弃
🚧 待实现
```

---

# M0–M6 状态矩阵

| Milestone | v8 状态 | 说明 |
|---|---|---|
| M0 工程基线 | ✅ | Full-Stack Template / Python 3.14 / PostgreSQL / Alembic / backend tests / frontend build / Playwright / Item reference / infra audit |
| M1 Settings | ✅ | Auth/User、Item、Docker/Compose/Traefik、DATABASE_URL、Redis/LiteLLM settings、deadline/guard、weak-secret fail-fast；LangGraph/LangSmith settings 已删 |
| M2 Redis | ✅ | Redis service / async client / lifespan / ping / namespace / ChatRuntime（IP rate、user rate、daily quota、user concurrency、Redis failure policy、cross-process cancellation） |
| M2 LiteLLM | ✅ | pinned gateway / logical model / primary-fallback / retry-timeout-cooldown / service key / token-cost-call-id / failure injection；LangChain adapter（`infra/llm.py`）已删 |
| M3 Generic Chat | 🟡→✅ | POST /api/v1/chat、Auth、request_id、ChatService orchestration、deadline、Product error mapping 保留；Agent 实现待 PydanticAI 接入（当前 503 AgentRuntimeNotConfigured） |
| M3 RuntimeClock | ✅ | `runtime_clock_context()` 纯函数保留；LangChain middleware 已删 |
| M4 Business Persistence | ✅ | conversation / message / request_run / migrations / indexes / CRUD / ownership / durable user message / terminal states / Request status API / persistence tests；`langgraph_thread_id` 已删 |
| M5 LangGraph Persistence | **SUPERSEDED** | AsyncPostgresSaver / checkpoint / thread mapping / LANGGRAPH_* 配置 / 相关依赖与测试全部移除；durability 需求重新分配（PostgreSQL / Redis Stream / DBOS） |
| M6 Product Lifecycle | ✅ | AuthZ（conversation/message/request ownership、IDOR tests）、Idempotency（user+key unique、running/completed duplicate、concurrent race、no duplicate cost）、Redis rate/quota/concurrency、Absolute deadline、Explicit cancellation、Request status/cancel API、terminal state race protection |

---

# v8 施工记录

## Commit 1 — 建立 Product / Agent 边界（已完成 ✅）

```text
refactor(v8): isolate agent execution from product runtime
- 新增 app/agent/executor.py（AgentExecutor port）
- ChatService 从 build_travel_agent 改为 AgentExecutor dependency
- API dependency 改为 get_agent_executor
- 测试 FakeAgent → FakeExecutor
- 旧 Deep Agents 通过 LegacyDeepAgentExecutor adapter 继续工作
```

## Commit 2 — 删除旧 Agent Framework（已完成 ✅）

```text
chore(v8): remove legacy deep-agents langgraph runtime
- 删除 agents/、infra/llm.py、infra/checkpoint.py
- 删除依赖：deepagents / langgraph / langgraph-checkpoint-postgres /
  langchain / langchain-openai / langsmith
- Settings / .env.example 清理 LangGraph/LangSmith
- lifespan 只初始化 Redis
- RuntimeClock 纯函数迁移至 app/agent/context/runtime_clock.py
- 删除旧框架 tests；test_litellm.py 保留 Gateway contract
```

## Commit 3 — 数据库 clean baseline（已完成 ✅）

```text
refactor(db): remove agent-framework identity from product schema
- Conversation.langgraph_thread_id + uq_conversation_langgraph_thread_id 删除
- Alembic migration c4f4d8a12b7e 同步修改
- persistence tests 更新
- app DB 重建 + alembic upgrade head 验证
```

## Commit 4 — 文档重基线（已完成 ✅）

```text
docs(v8): rebaseline milestones around product runtime
- AGENTS.md / 架构v8.md / 施工路线图.md 重写为 v8 矩阵
- Product/Infra 契约文档去框架化
- 删除 v7 handoff/report 与架构v7.md
```

---

# Clean Baseline HARD GATE（已通过 ✅）

```text
git grep -nEi 'deepagents|deep_agents|langgraph|langchain|langsmith|langgraph_thread_id' backend .env.example
→ 0 个旧 Runtime 引用

uv lock → OK
ruff check → OK
mypy → OK
ty check → OK
pytest → 全绿
alembic upgrade head → OK（Conversation 无 langgraph_thread_id，无 langgraph schema）
```

---

# 后续路线图

## 下一阶段：Pydantic AI Core

```text
feat(agent): introduce pydantic ai executor
ChatService → AgentExecutor → PydanticAIExecutor → Pydantic AI → LiteLLM
- 不使用 Harness / Streaming / DBOS / Travel SubAgents
- 先恢复 M3 普通问答
- 重新跑 M0–M6 验收
```

## v8 后续 Milestones

```text
M7  Business Usage Attribution
M8  PydanticAI + Vercel AI Streaming
M9  C-End Chat
M10 Observability / Security / Eval
M11 Production Durability / HA（DBOS 决策）
```
