# Domain
# This would be set to the production domain with an env var on deployment
# used by Traefik to transmit traffic and acquire TLS certificates
DOMAIN=localhost
# To test the local Traefik config
# DOMAIN=localhost.tiangolo.com

# Public frontend URL, used by the backend to generate links in emails.
# Use the FastAPI application URL for local Docker Compose; deployments use the deployed application URL.
FRONTEND_HOST=http://localhost:8000
# In staging and production, set this env var to the frontend host, e.g.
# FRONTEND_HOST=https://example.com

# Environment: local, staging, production
ENVIRONMENT=local

PROJECT_NAME="Full Stack FastAPI Project"
STACK_NAME=full-stack-fastapi-project

# Backend
BACKEND_CORS_ORIGINS="http://localhost,https://localhost,http://localhost.tiangolo.com"
SECRET_KEY=changethis
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=changethis

# Emails
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=info@example.com
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587

# Postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_HOST_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changethis

SENTRY_DSN=

# Configure these with your own Docker registry images
DOCKER_IMAGE_BACKEND=backend

# Shared runtime / AI infrastructure
# Redis 连接，用于缓存和共享状态。
REDIS_URL=redis://redis:6379/0
REDIS_KEY_PREFIX=travel_agent:
# LiteLLM Proxy 地址。
LITELLM_BASE_URL=http://litellm:4000
# FastAPI 调用 LiteLLM 的服务凭证。
LITELLM_SERVICE_KEY=sk-local-travel
# 应用使用的逻辑模型名。
LLM_LOGICAL_MODEL=travel-agent-llm
# LangGraph checkpoint 数据库连接。
LANGGRAPH_DATABASE_URL=postgresql://postgres:changethis@db:5432/app
LANGGRAPH_DATABASE_SCHEMA=langgraph
LANGSMITH_TRACING=False
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=travel-agent-v7
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LITELLM_IMAGE=ghcr.io/berriai/litellm@sha256:90d8de0ea6fbb3cad145d1019d00a0149ae400b1e18e2011a60f1988f143f672
LITELLM_MASTER_KEY=sk-local-master
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_KEY=

# LiteLLM 日志与成本数据库连接。
LITELLM_DATABASE_URL=postgresql://postgres:changethis@db:5432/litellm

# Optional Doubao (Volcengine Ark, OpenAI-compatible)
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
DOUBAO_API_KEY=

# Application runtime
# 应用环境：local/staging/production。
APP_ENV=local
# 调试开关：生产环境应为 False。
APP_DEBUG=False
# RuntimeClock 默认时区。
APP_TIMEZONE=Asia/Shanghai
# 请求截止时间（秒）。
REQUEST_DEADLINE_SECONDS=60

# Database contracts
# FastAPI 业务数据库连接。
DATABASE_URL=postgresql+asyncpg://postgres:changethis@db:5432/app

# Agent guards
# Agent 最大递归步数。
AGENT_RECURSION_LIMIT=25
# 单请求模型调用上限。
MODEL_CALL_LIMIT=20
# 单请求工具调用上限。
TOOL_CALL_LIMIT=50

# Optional infrastructure tracing (LangSmith remains the primary AI trace UI)
OTEL_SERVICE_NAME=travel-agent-api
OTLP_ENDPOINT=

# Travel tool providers (keep provider keys out of the FastAPI LLM path)
TAVILY_API_KEY=
AMAP_API_KEY=
WEATHER_API_KEY=
